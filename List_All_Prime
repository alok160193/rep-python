def list_all_prime(start_num,end_num):
    for num in range(start_num, end_num+1):
        if num > 1:
            #check for factors
            for i in range(2,num):
                if (num%i) == 0:
                    break
            else:
                print(num)
